'''A tuple is a built-in data type in Python that is used to store a collection of items. 
Tuples are ordered, immutable, and allow duplicate values .'''
'''Tuples are defined by enclosing the items in parentheses () and separating them with commas.'''

tup = (5,3,8,1,4)
print(tup[0])
# tup[0] = 10 # we cannot change the value of tuple in python because it is immutable

print(tup[0:3]) # it will print the elements from index 0 to index 2
print(tup[:3]) # it will print the elements from index 0 to index 2
print(tup[2:4]) # it will print the elements from index 2 to
